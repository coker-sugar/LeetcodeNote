## 每日小记

### 2024年9月10日

#### 1. CSS

1. BFC是什么？怎么设置？BFC特性？display：inline-block与overflow: hidden区别？
2. flex有哪些属性？flex:1？1 1 0%
3. display:none; visibility:hidden; opacity:0;区别？结构&继承&性能
   - visibility支持tansition吗？
   - 如果支持，可以用来做什么？
4. 单行文本溢出？多行文本溢出？兼容多行文本溢出？js实现？overflow:hidden贯穿始终
5. transition有哪些属性？animation有哪些属性？前四个一样（property—>name）
6. 如何实现三角形？
7. 清除浮动的三种方式？父盒子内部有2个浮动的子盒子，但是子盒子的高度没有把父盒子撑起来？clear原理？
8. css3新增属性？



#### 2. JS

1. 循环打印黄蓝绿-promise
2. 每隔1s打印1，2，3，4
3. 小孩报数
4. 字符串不重复最长长度
5. 实现jsonp
6. 大数相加

#### 3. 回顾加强

1. BFC特性

2. flex属性：

   - 容器：
     1. justify-content、align-center
     2. flex-direction + flex-wrap = flex-flow
   - 项目：
     1. align-self
     2. flex
     3. flex-grow
     4. flex-shrink
     5. flex-basis

3. animation-iteration-count / direction / fill-mode / play-state

4. css3新增属性
   1. 选择器：
      - 属性选择器
      - nth-child，nth-of-type
   2. 边框
      - border-radius
      - box-shadow
   3. 过渡和动画
   4. 响应式布局
      - flex
      - grid
      - 媒体查询
   5. 背景
      - size
      - origin
   
5. 小孩报数：

6. 小孩报数：传入n，target -->
   1. 喊到target就删除 —— 有个数组arr —— 有个index下标
   2. 怎么喊到target，有个计数器
   3. 什么时候知道删除了？ 有个计算删除数的 sum 

7. 字符串不重复最长长度：map

8. 清除浮动的clear方式：

   ```css
   .clearfix:after {
       content: '';
       display: block;
       clear: both;
   }
   ```

   clear：元素的边不能和前面的浮动元素相邻 ——> 确保包含 `clear` 属性的元素不会被浮动元素覆盖，使得该元素位于所有浮动元素的下方，避免了布局问题，但浮动元素本身依然会保持在脱离文档流的状态。

### 2024年9月11日

#### 1. CSS

1. z-index怎么生效的？默认层叠顺序？什么时候不生效？怎么把子盒子放在父盒子下方？
1. css会堵塞dom树渲染吗？堵塞后面的js执行吗？如何提高加载css速度？
1. 在浏览器的渲染过程中，预加载扫描器是干嘛的？重绘包括什么？这期间重绘怎么把样式计算应用到屏幕上的？JS什么时候解析的？dom解析和css解析是同步的吗？重绘完毕后整个渲染流程就走完了吗？
1. css预处理器？sass、less & stylus区别？
1. 1px的线怎么画？0.5px呢？为什么1px会显得宽？
1. switch按钮

#### 2. JS

1. commonjs 和 esmodule区别？

#### 3. 回顾加强

1. z-index

   [为什么我写的z-index不生效？ - 南玖的前端blog - SegmentFault 思否](https://segmentfault.com/a/1190000042584801)

    <img src="C:\Users\21174\AppData\Roaming\Typora\typora-user-images\image-20240912145533577.png" alt="image-20240912145533577" style="zoom:67%;" />

   - z-index单独不生效，要配合定位写才生效（不是默认的static）——>**`position`属性为**非 **`static`值并设置`z-index`属性为具体数值才能产生层叠上下文**

   - css3以后，flex布局时z-index也可以生效

1. 1px线太宽的原因？

   <img src="C:\Users\21174\AppData\Roaming\Typora\typora-user-images\image-20240912153259445.png" alt="image-20240912153259445" style="zoom:80%;" />

   在普通屏，1个css像素只用1个物理像素呈现；2倍屏中，1个css像素会用4个物理像素；3倍屏中则是9个。

   解决：

   1. 0.5px
   2. 先放大后缩小：height:200%; transform: scaleY(0.5);
   3. 用viewport缩放

   ```html
   <meta name="viewport" content="initial-scale=0.5, maximum-scale=0.5, minimum-scale=0.5, user-scalable=no">
   ```

   

1. commonjs用module.export导出，require导入；ES6用export导出，import导入

   commonjs导出的是一个值的**拷贝，**ES6导出的是一个值的索引

   commonjs同步加载模块，ES6是异步加载，有一个独立的模块依赖的解析阶段

### 2024年9月12日

#### 1. HTML

1. src&href区别？  

   应用在哪个标签？是否同步？是否下载资源？

2. link和@import区别？ 3点

3. HTML语义化？ 
   1. 是什么？
   2. 有什么？
   3. 结构更清晰？三大优点：对搜索引擎、对运行工具、对开发者

4. script中defer & async区别？

   顺序 & 异步

5. 行内 & 块级元素？li是块级吗？link是行内吗？

   行内没有text！有span

#### 2. CSS

1. css选择器和优先级？

   - 有什么和类选择器同级？

   - 有什么和标签选择器同级？

   - 介于标签选择器 & 通配符选择器之间的有哪些选择器？	

   - '+' 与 '~' 选择器有什么不同？

2. display属性值和作用？
   - inline-block默认宽度？可以设置宽高吗？
3. block、inline、inline-block区别？
   - inline可以设置margin & padding吗？
   - inline-block可以设置margin & padding吗？
4. 隐藏元素的方法？
5. 盒模型理解？
   - IE盒模型的width组成？
6. 两栏布局？左固定，右自适应
7. 三栏布局？中间自适应
8. position有哪些属性？
   - fixed & static区别？
   - inherit干嘛的？

#### 3. JS

1. create特点？如何实现？

2. instanceof实现

3. new做了什么？如何实现？注意会把原型给对象

4. 防抖使用场景？怎么实现？

5. 节流使用场景？怎么实现？

6. 什么是伪数组？常见的？怎么转真数组？Array.of()

7. 有了expires ，为什么还要有max-age？

8. type & interface 区别？

9. 泛型的理解？

#### 4. 回顾加强

1. link是页面载入同时加载；@import是等页面完全载入以后再加载

   linl支持js操作dom来改变样式，因为link是一个标签，而@import是在css内，js操作不了

2. 类数组转为真数组：

   ```javascript
   Array.prototype.slice.call(arguments);
   [...arguments];
   Array.from(arguments);
   ```

### 2024年9月13日

#### 1. HTML

1. DOCTYPE的作用？

   - 作用：浏览器以什么样的文档类型解析文档
   - 严格模式 & 怪异模式：是否以最高标准呈现页面？哪个有标准盒模型？怎么变成怪异模式？2种方式，没有 URI 的过渡 DTD
   - 注意和use strict开启的严格模式是不同的概念

2. webworker

   - worker能操作dom吗？能用window吗？能读取本地文件吗？
   - 基本用法：构建，发送数据，监听数据，终止（注意：发送的数据是复制的，不是引用的，所以即使主线程修改内容，worker线程也不会被改变）
   - worker的API
   - worker内部实现：怎么监听message？怎么发数据出去？怎么监听错误？怎么关闭？（终止terminate()，关闭close() ）
   - webworker使用场景？——>怎么做长轮询？

   [Web Worker 使用教程 - 阮一峰的网络日志 (ruanyifeng.com)](https://www.ruanyifeng.com/blog/2018/07/web-worker.html)

3. head标签有哪些？其中什么是唯一不可少的？

4. drag的相关API？被拖动元素 、 目标元素

#### 2. 回顾加强

1. worker监听数据：除了使用`self.addEventListener()`指定监听函数，也可以使用`self.onmessage`指定

2. worker内部怎么加载脚本？ importScript('s1.js', 's2.js');

3. worker的arrayBuffer有什么用？

   - `ArrayBuffer` 允许你直接操作原始的二进制数据。这对于处理像图像、音频文件或其他数据格式非常有用，因为你可以按字节操作数据，而不是通过更高层次的抽象处理。

   -  `ArrayBuffer` 支持“传递”语义，而不是“复制”。这意味着数据可以在不同线程之间传递，而不会复制数据，从而节省内存和提高性能

4. worker实现轮询

   ```js
   function createWorker(f) {
     var blob = new Blob(['(' + f.toString() +')()']);
     var url = window.URL.createObjectURL(blob);
     var worker = new Worker(url);
     return worker;
   }
   
   var pollingWorker = createWorker(function (e) {
     var cache;
   
     function compare(new, old) { ... };
   
     setInterval(function () { // setInterval 一秒，向后端发起请求拿到最新值，比对新旧data，有更新就告知主线程
       fetch('/my-api-endpoint').then(function (res) {
         var data = res.json();
   
         if (!compare(data, cache)) {
           cache = data;
           self.postMessage(data);
         }
       })
     }, 1000)
   });
   
   pollingWorker.onmessage = function () {
     // render data
   }
   
   pollingWorker.postMessage('init');
   ```

### 2024年9月14日

#### 1. 场景题：

1. 大文件上传？

   - 使用webworker线程计算hash

   - 通过arraybuffer避免结构化克隆
   - 实现进度条？
   - 中断重传？
   - 启用workerpool复用worker，避免每次分片都要重新开一个worker
   - 实现并行上传
   - https://juejin.cn/post/7353106546827624463?share_token=e7bc6104-767c-4046-9406-44b68cc71b30
   - https://juejin.cn/post/7273429629398515746?share_token=aacb6c94-4c12-451f-83b9-40df677b0271
   - https://link.juejin.cn/?target=https%3A%2F%2Fgithub.com%2FTkunl%2Fkun-uploader%3Fmode%3Dlight
     https://link.juejin.cn/?target=https%3A%2F%2Fgithub.com%2FTkunl%2Fkun-uploader-server%3Fmode%3Dlight

2. 实现适配？

   - 媒体查询+vw vh实现适配

   - 图片精度问题？针对不同像素密度，使用媒体查询，选择不同精度的图片，避免图片失真

   - 头部meta：

     ```html
     <meta name="’viewport’" content="”width=device-width," initial-scale="1" maximum-scale="1" user-scalable="no"/>
     ```

3. 一键换肤？

   - 两套css+切换class
   - 使用less变量+切换变量

#### 2.笔试题

```javascript
function show() {
    console.log(this);
}
show.call(null)

var a = []
a[0] = 1;
a[2] = 2;
console.log(a.length); 

let person = {
    name:'zs',
    sayHello:function(){
        console.log(this.name);
    }
}
let another = Object.create(person);
another.name = 'lisi';
another.sayHello(); 

let num1 = 5;
let num2 = '10';
console.log(num1 + num2); 
console.log(num2 + num1); 
```

### 2024年9月15日

#### 1. CSS

1. 有继承属性的值？

   - 字体：font-weight; font-size; font-size; font-style;
   - 文本：text-align; line-height; text-indent; letter-spacing; word-spacing
   - visibility
   - list-style
   - cursor

2. 对精灵图的理解？怎么做？优点？缺点？

3. 物理像素？逻辑像素？像素密度？

   1242 * 2688

   414px * 896px

   1242 / 414 = 3 密度

4. 如果标签没有设置height，那么该标签的高度由谁决定？ Line-height

5. css优化？

   - 请求
   - **选择器**
   - 渲染
   - **复用**

6. **postCss**是什么？后处理器

   - 哪个插件自动加前缀？autoprefixer
   - 原理？
   - 怎么实现？
   - [PostCSS 从入门到精通，看着一篇就够了（1）-CSDN博客](https://blog.csdn.net/nljycg/article/details/138196855)

7. 伪元素和伪类区别

   - 伪元素？两个冒号，不会生成新的dom但是会新加内容在页面中
   - 伪类？一个冒号，元素状态改变时添加一些css效果

8. px & rem & em区别？

   - em是相对于谁来设置字体大小的？

#### 2. 回顾

1. 对精灵图的理解
   - 利用CSS的 background-image，background-repeat，background-position属性的组合进行背景定位
   - 优点：减少网络请求，提高性能；减少图片字节大小，三张图片合成一张图片字节总是小于三张
   - 缺点：开发时需要photoshop这类工具去测距；一旦页面背景有改动，就需要改变图片定位
2. css优化
   - 请求：css合并一个文件；link而非@import
   - 选择器：减少通配符使用，减少后端选择器，减少权重计算
   - 渲染：减少css嵌套层级，值为0的不加单位减少单位解析，
   - 复用：相同属性抽离出来整合在一个class中；将css放入外部代码中，并行加载

### 2024年9月16日

#### 1. JS

1. 数据类型？新增了2个？区别？
2. 数据类型的检测方式？typeof NaN？instanceof实现方式？
3. null & undefined区别？
4. typeof null？原理？前三位
5. 0.1 + 0.2 ？ 为什么？ 解决方式？
   - 计算机使用二进制系统来表示数字，而某些十进制的小数（例如 `0.1` 和 `0.2`）受到计算机长度限制，无法被精确地表示为二进制，二者相加时得到的是一个接近于0.3的数 
6. isNaN和Number.isNaN区别？
7. undefiend转为数字？null转为数字？string转为数字（非数字？空串？'0' == 0）symbol转为数字？
   - Symbol 类型的值不能转换为数字，会报错。
8. 0可以被转为false吗？NaN可以吗？
9. || 和 && 操作符的返回值？
10. Object.is() 和 “==”同还是"==="同？它的特殊情况：-0 & 0相等吗？NaN & NaN相等吗？
11. 隐式类型转换：
    - +
    - -、*、/
    - ==
    - \>、\<
12. let、var & const的区别？
13. 箭头函数 & 普通函数区别？
    - 写法？
    - 构造函数？

#### 2. 回顾

1. undefiend转为数字是NaN

2. 类型转换

   除了Date对象默认为string类型，先toString再valueOf；

   其它的都视为number类型，ToPrimitive通过valueOf返回原始值，没有返回则用toString返回

   ```js
   class CustomObject {
       constructor(value) {
           this.value = value;
       }
       
       valueOf() {
           return this.value;
       }
   }
   ```

   通过*ToPrimitive*返回基本数据类型再操作

3. 类型转换

   - +有串转串，无则转数字

   - /、*、—、== 转数字
   - \>、\<有串转字母序列，`'b' > 'a'`，无则转数字

4. let&const不可以重复声明变量，var可以

### 2024年9月18日

#### 1. JS

1. map & object区别？

   - 键名限制
   - 键名遍历(entries、for...in)
   - 获取长度
   - 增删性能

2. map & weakMap区别？map有哪些操作方法？weakmap缺少了什么？map遍历方式？weakMap可以遍历吗？

   - 但因为 `WeakMap` 不允许观察其键的生命周期，所以其键是不可枚举的。没有方法可以获得键的列表。如果有的话，该列表将会依赖于垃圾回收的状态，这引入了不确定性。如果你想要可以获得键的列表，你应该使用 [`Map`](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Map)。
   - WeakMap **不支持**迭代以及 keys()， values() 和 entries() 方法。 所以没有办法获取 WeakMap 的所有键或值。

3. 数组的方法？

   - 转为字符串
   - 尾部操作
   - 首部操作
   - 排序
   - 复制：concat，slice，splice
   - 查找
   - 归并

4. 字符串的方法？

   - 访问：charAt、str[index]
   - 长度：length
   - 查找：indexOf、includes、startWith、endWith
   - 截取：slice、substring、substr（前2个范围，后面1个截取个数）、trim
   - 转换大小写：toUpperCase、toLowerCase
   - 替换：replace、replaceAll
   - 分割与连接：split、concat

5. 用&代替判断偶数？

6. 什么是dom？什么是bom？bom内置对象有什么？

7. js为什么要变量提升？有什么问题？

   - 解析和预编译过程中的声明提升可以提高性能，让函数可以在执行时预先为变量分配栈空间
   - 声明提升还可以提高JS代码的容错性，使一些不规范的代码也可以正常执行

   问题：函数内部声明的变量可能覆盖外部变量

8. use strict带来哪些限制？针对变量 & 针对this

   - this不指向全局对象，为undefined
   - 不可以使用delete删除变量或者函数
   - 禁止重复声明变量
   - 禁止使用全局变量：未声明的变量不能被直接赋值

9. 解释型 & 编译型语言的区别？

   - 性能：逐行解释并执行，速度慢；先编译为机器码，生成可执行文件再执行
   - 平台：只需要安装解释器就可以运行，跨平台性强；需要特定平台来单独编译，再运行

10. for in & for of

11. ajax & axios & fetch区别？

    - ajax特点是实现了局部数据刷新，基于XMLHttpRequest对象实现的，但是XML本身架构就是不清晰的，已经在逐步淘汰（XML不清晰：**允许自定义标签**，导致不同文档语义不一致；XML数据都是以文本类型存储，**缺乏明确的数据类型**，如布尔、数字）
    - axios是基于ajax封装的，支持Promise API、取消请求、可以**自动转换json数据，方便的设置请求超时**，是现在经常使用一种网络连接方式
    - fetch是es6提出的基于Promise设计的，支持async、await，语法也更加简洁；但是featch不支持abort，需要手动转换json数据，不支持超时控制，相较于axios没有那么完善

12. fetch如何做超时控制？

```js
function fetchWithTimeout(url, options = {}, timeout = 5000) {
    return Promise.race([
        fetch(url, options),
        new Promise((_, reject) =>
            setTimeout(() => reject(new Error('请求超时')), timeout)
        )
    ]);
}

// 使用示例
fetchWithTimeout('https://api.example.com/data')
    .then(response => {
        if (!response.ok) throw new Error('网络错误');
        return response.json();
    })
    .then(data => console.log(data))
    .catch(error => console.error(error.message))
```

12. 原型 & 原型链
13. 如何获取非原型链上的属性？
14. 对闭包理解？使用场景？
    - 防抖、节流函数
    - 函数柯里化
    - 发布订阅模式
15. 作用域链本质是一个指向变量对象的指针列表
    - 在当前作用域中查找所需变量，但是该作用域没有这个变量，那这个变量就是自由变量。如果在自己作用域找不到该变量就去父级作用域查找，依次向上级作用域查找，直到访问到window对象就被终止，这一层层的关系就是作用域链。

#### 2. 动态规划

1. 装满背包，物品可以重复利用，最少要装几次？
   - 公式：`dp[i] = min(dp[i - coins[j]] + 1, dp[i]);`
   - 遍历顺序：先遍历背包，再遍历物品——>因为目的是把当前这个背包装满，要求最少次数

#### 3. 扫码登录怎么做？

![image-20240918214311991](C:\Users\21174\AppData\Roaming\Typora\typora-user-images\image-20240918214311991.png)

#### 4. 回顾

1. weakmap注意区分键名 & 键值，且键值是强引用的！
   - WeakMap 的键值是强引用。即使键名被垃圾回收机制回收了，键值仍然会存在，因为 WeakMap 保持对键值的强引用。这意味着在键名被回收后，键值依然有效，直到其他引用将其也回收。
2. for in 还会遍历整个原型链！性能很差，本身设计就是为了遍历对象的
3. ajax、axios、fetch
   - ajax：XML架构不清晰，文本型存储没有明确的数据类型 & 支持自定义标签，不同文档语义不一致
   - axios：支持 Promise API、请求中断、超时控制、可以自动转换json数据格式
   - fetch：es6基于Promsie实现，支持async、await，语法简洁；不支持abort、需手动转json、不支持超时控制

### 2024年9月20日

#### 1. JS

1. this指向怎么判断？

   - 函数调用？

   - 方法调用？

   - new创建？

   - apply，call，bind改变this指向？哪个是永久的？

     `bind` 会创建一个新的函数，永久地将 `this` 指向指定的对象，无法更改。

2. call & apply实现方式？

3. bind 实现方式？

4. promise的理解？

   - 异步编程的一种解决方案
   - 三种状态，通过谁转变？
   - 五个方法

5. 到await那一步会执行吗？

6. async 对比 promise优势？

   - 更简洁易读
   - 类似同步代码
   - 避免深层嵌套和回调地狱

7. 垃圾回收机制

   - 内存结构：新生代 & 老生代

   - 压缩内存碎片？

     1. 新加入的对象都会存放到对象区域，当对象区域快被写满时，就需要执行一次垃圾清理操作。
     2. 在垃圾回收过程中，首先要对对象区域中的垃圾做标记；标记完成之后，就进入垃圾清理阶段，副垃圾回收器会把这些存活的对象复制到空闲区域中，同时它还会把这些对象有序地排列起来，所以这个复制过程，也就相当于完成了内存整理操作，复制后空闲区域就没有内存碎片了。
     3. 完成复制后，对象区域与空闲区域进行角色翻转，也就是原来的对象区域变成空闲区域，原来的空闲区域变成了对象区域。

   - 新生代的内存空间大小？

     但复制操作需要时间成本，如果新生区空间设置得太大了，那么每次清理的时间就会过久，所以为了执行效率，一般新生区的空间会被设置得比较小。

     也正是因为新生区的空间不大，所以很容易被存活的对象装满整个区域。为了解决这个问题，JavaScript 引擎采用了**对象晋升策略**，也就是经过**两次垃圾回收**依然还存活的对象，会被移动到老生区中。

   - 老生代清除内存的方法？标记-整理？

     - 首先是标记过程阶段。标记阶段就是从一组根元素开始，递归遍历这组根元素，在这个遍历过程中，能到达的元素称为活动对象，没有到达的元素就可以判断为垃圾数据。
     - 接下来就是垃圾的清除过程。它和副垃圾回收器的垃圾清除过程完全不同，对一块内存多次执行**标记 - 清除**算法后，可能会产生大量不连续的内存碎片。
     - 而碎片过多会导致大对象无法分配到足够的连续内存，于是又产生了另外一种算法——**标记 - 整理（Mark-Compact）**，这个标记过程仍然与标记 - 清除算法里的是一样的，但后续步骤不是直接对可回收对象进行清理，而是**让所有存活的对象都向一端移动，然后直接清理掉端边界以外的内存。**

     ![image-20240921001901271](C:\Users\21174\AppData\Roaming\Typora\typora-user-images\image-20240921001901271.png)

     ![image-20240921001909491](C:\Users\21174\AppData\Roaming\Typora\typora-user-images\image-20240921001909491.png)

   - 什么是全停顿？如何解决？

     - 由于 JavaScript 是运行在主线程之上的，一旦执行垃圾回收算法，都需要将正在执行的 JavaScript 脚本暂停下来，待垃圾回收完毕后再恢复脚本执行。我们把这种行为叫做**全停顿（Stop-The-World）**。
     - 在 V8 新生代的垃圾回收中，因其空间较小，且存活对象较少，所以全停顿的影响不大，但**老生代**就不一样了。如果在执行垃圾回收的过程中，占用主线程时间过久，将会**造成页面卡顿**。
     - 为了降低老生代的垃圾回收而造成的卡顿，V8 将标记过程分为一个个的子标记过程，同时让垃圾回收标记和 JavaScript 应用逻辑交替进行，直到标记阶段完成，我们把这个算法称为**增量标记（Incremental Marking）** 算法。

8. setTimeout只能接受两个参数吗？

   ```js
   setTimeout((arg) => {
       console.log(arg);
   }, 1000, 'Hello');
   ```

    其实可以接受三个参数，但第三个参数是用于传递参数到回调函数的。

9. [a-Z]可以表示所有字母吗？\d可以代替[0-9]吗
   
   - 不可以，正确的表示法 `[a-zA-Z]`
   - 可以
   
10. 子网掩码？255.255.255.0有多少个主机？
   - 网络和主机
   - 256-2（网络地址 0 & 广播地址 255）

### 2024年9月21日

#### 1. VUE

1. 双向数据绑定

   1. 理解

      - 在observe中构建dep（dep会遍历watcher通知更新数据），通过Object.defineProperty进行数据劫持，在getter中把watcher加入到dep中，setter时调用dep.notify()通知dom上的数据更改；

      - 在compile中拿到有数据绑定的dom（递归拿到子dom），拿到dom上的数据并构造一个监听器watcher（在watcher中除了有回调函数修改数据，还会遍历整个数据集（通过get）将watcher加入到dep中）

      - 上面是数据更新视图，视图更新数据怎么做？

        通过监听事件监听数据变化，如果变了，就会调用setter更改数据，同时setter中的dep.notify()也被调用，也就更新了视图

   2. 原理答案：

      - observe递归遍历数据对象，通过defineProperty给属性加上get & set，在数据更新时会触发setter
      - compile模板解析会将模板变量替换成数据，然后初始化渲染页面视图，并将每一个指令对应的节点绑定到更新函数中，添加监听数据的观察者，只要数据变化，就会更新视图
      - watcher会在实例化时 加入到dep中（三行经典代码）；自身有update函数；dep.notify时调用update函数更新数据

   3. 糅合：

      Vue.js 是采用**数据劫持**结合**发布者-订阅者模式**的方式，通过Object.defineProperty()来劫持各个属性的setter，getter，在数据更新时，在setter中触发订阅者的更新函数更新数据，实现视图层和数据层的数据统一。

2. defineproperty缺点？给vue2对象添加属性不刷新的 解决方案？

   - Vue.$set的原理？
     1. 如果是数组，用slice添加
     2. 如果是对象，用 hasOwnProperty 判断属性在对象中是否存在，存在就直接更改；不存在就再调用一次defineReactive给属性添加getter和setter

3. MVC & MVP & MVVM 区别？
   - C：controller，用户操作页面，视图层更新 通知 数据层更新数据；缺点：紧耦合；Model层没有分离存储 & 更改
   - P：presenter，数据更改操作放在P中，实现视图层 & 数据层解耦；缺点：还需要手动拿到dom来操作，不利于开发和维护
   - VM：ViewModel实现了双向数据绑定，视图更新可以自动更新数据，数据更新也可以自动更新视图

4. computed & watch？

   - 区别？
     - 走缓存、不支持异步、依赖多个属性计算
     - 支持异步、深度监听、立即执行、监听单个属性

   - 使用场景？
     - 多个数据影响一个：computed格式化显示、过滤（类似vue2的filter）、复杂计算；
     - 一个数据影响多个：watch异步操作、数据持久化

   知道：

   - 为什么computed不支持异步？
   - watch能写多个监听吗？
   - watch默认能侦听到嵌套属性吗？ 
   - 有同时监听多个值的函数吗？
   - watchEffect和watch区别？

   答案：

   - 从设计上解释：根据多个属性计算而来，需要一个**准确的值**，但是**异步事件是不确定执行时间**的，值也是不确定的，绑定在监听的这个值的其它属性无法同步更新，这就不符合设计computed的初衷

   - 能，挨个写
   - 不能，需要deep
   - 有，watchEffect
   - watch能侦听新值和旧值，默认情况下只在数据变化才触发回调（懒监听，创建监听器时就触发：加上immediate）；watchEffect不能侦听新值和旧值，能自动追踪到能访问到的响应式数据

5. slot

   常问：

   - 含义？占位符，调用子组件时，用来放子组件包裹的内容；
   - 分类？具名插槽，作用域插槽：带数据的插槽，用于父子组件传值
   - 使用场景？封装组件

   亮点：

   - 本质？传入一个对象，对象有多个钩子函数，每个函数就是一个插槽，组件在使用插槽时就是在调用函数，生成相应的dom渲染在子组件上

### 2024年9月22日

#### 1. Vue

1. 过滤器

   - 是什么？

   - 怎么用？

     ```js
     filters: {
       changeData: (val) => {
         return `￥${val}元`
       }
     }
     
     {{ price | changeData }}
     ```

   - vue3支持吗？ 

   已废弃，用函数替代了

2. vue的组件如何缓存？

   - localStorage、sessionStorage

   - keep-alive

     1. 是什么？

        当被缓存的组件跳转到其它组件时，被缓存组件实例是不会被销毁的，只是移除了它的渲染dom。当再次回到缓存的组件时，会直接从缓存中取出这个组件，而不是重新渲染

     2. 如何实现？router.meta: { keep-alive: true }; keep-alive 缓存组件

     3. 页面缓存后，重新进入要再发一次请求怎么做？ activated

     4. [VUE工程的keep-alive缓存机制（全部缓存、部分缓存、定向缓存）_vue缓存keep-alive-CSDN博客](https://blog.csdn.net/happy81997/article/details/103345417)

     5. 如何控制内部 要缓存具体哪些子组件？如何控制缓存的最大数量？ : include、: max

     6. 实现原理？

        - 根据include、exclued来监测要缓存的组件？
          - 不需要缓存的直接返回vnode实例
          - 需要缓存的，会看是否在cache数组中？
            - 存在：会删除原来位置上的key，再push key到keys数组末尾，以便清除最不常用的组件（LRU）
            - 不存在：将vnode的key存入keys数组，并把vnode实例存入cache数组中，若超过max长度？
              - 删除第一个缓存的vnode实例

3. 修饰符？

   - 含义？

     指令的后缀

   - 分类？

     1. 事件：prevent、stop、captrue、once、passive

        @click.passive ： 不调用prevent事件，会执行默认事件

     2. 按键：enter、down、up、space、left、right、shift、alt

        @keydown.enter="handleKey"

     3. 鼠标：left、right、middle

        @click.left

4. v-if & v-show的区别？

   - 切换本质：display:none; 还是存在这个dom，只是隐藏了不会加入dom树
   - 切换性能
   - 初始渲染性能
   - 使用场景：按钮权限控制，频繁切换

5. v-if & v-for优先级？

   - 在vue2中v-for的优先级高于v-if，而在vue3中v-if的优先级要高于v-for。

6. v-model是怎么实现的？

   ```vue
   <input v-model="searchText">
   // 相当于
   <input
     v-bind:value="searchText"
     v-on:input="searchText = $event.target.value"
   >
   ```

7. nextTick

   - 含义：异步更新数据后获取dom

   - 为什么要有它：数据更新和dom更新都是异步的。Vue在修改数据后，视图不会立刻更新，而是等**同一事件循环**中的所有数据变化完成之后，再统一进行视图更新。所以如果想在数据更新后立即拿到更新数据后的dom，就可以利用nextTick开启一个异步任务（一般是微任务promsie | mutationObserver，不支持就用宏任务）添加到任务队列中，等nextTick前面的任务执行完毕后，nextTick 的微任务就会执行，调用传入的回调函数，来操作最新dom

   - 怎么用？async...await nextTick() | nextTick( () => {} )

   - 场景？在created生命周期 中操作dom 、 改变dom数据后对dom进行操作

   - 原理？

     ```js
     const callbacks = [];
     let waiting = false;
     
     function nextTick(callback) {
         // 将回调函数添加到队列中
         callbacks.push(callback);
         
         // 如果尚未在等待微任务执行
         if (!waiting) {
             waiting = true;
             // 使用 Promise 创建微任务
             Promise.resolve().then(flushCallbacks);
         }
     }
     
     function flushCallbacks() {
         // 重置等待状态
         waiting = false;
         
         // 依次执行所有回调函数
         while (callbacks.length) {
             const cb = callbacks.shift(); // 取出第一个回调
             cb(); // 执行回调
         }
     }
     ```

8. SPA & MPA

   - 含义？
   - 区别？
     1. 结构：一个主页面 + 多个模块组件 & 多个完整页面
     2. 切换：hash、history & 链接跳转
     3. 刷新：局部刷新，切换更快 & 整体刷新，网速较差时切换慢
     4. 数据传递：便捷，vuex & localstorage、sessionstorage、url参数
     5. SEO
     6. 初始加载消耗：开销高，统一加载所有js和css，部分组件按需加载；初始加载消耗相较较低，但每一个加载消耗差不多，整体算下来消耗比SPA开销更大

9. hash & history[【面试】前端路由hash和history的区别_history hash服务器压力哪个大-CSDN博客](https://blog.csdn.net/qq_42345108/article/details/124144047)

   - 区别？
     1. 锚点？	
     2. 与服务器？
     3. 兼容？支持低版本和EI
     4. 原理？
     5. 历史记录？
   - 使用选择？
     1. 是否SEO
     2. 对兼容性的要求
     3. 后端是否配合
   - history访问子路由报404怎么办？重定向
   - 如何监听$route变化？ watch监听（加deep）OR window.location.href
   - $route & $router区别？ [Vue router 与 route 的区别_router route-CSDN博客](https://blog.csdn.net/x550392236/article/details/137717284#:~:text=本文详细解释了Vue)
   - 如何设置动态路由？如何传值？

   ```jsx
   //在APP.vue中
   <router-link :to="'/user/'+userId" replace>用户</router-link>    
   
   //在index.js
   {
      path: '/user/:userid',
      component: User,
   },
   ```

   - route生命周期？

     1. 全局路由钩子：beforeEach、beforeResolve、afterEach
     2. 组件路由钩子：beforeRouteUpdate、beforeRouteEnter、beforeRouteLeave，都有to、from、next三个参数

     - beforeRouteLeave：路由组件的组件离开路由前钩子，可取消路由离开。

     - beforeEach：路由**全局**前置守卫，可用于登录验证、全局路由loading等。

     - beforeEnter：路由独享守卫

     - beforeRouteEnter：路由组件的组件进入路由前钩子。

     - beforeResolve：路由**全局**解析守卫

     - afterEach：路由**全局**后置钩子

     

     - beforeCreate：组件生命周期，不能访问tAis。
     - created;组件生命周期，可以访问tAis，不能访问dom。
     - beforeMount：组件生命周期
     - deactivated：离开缓存组件a，或者触发a的beforeDestroy和destroyed组件销毁钩子。
     - mounted：访问/操作dom。
     - activated：进入缓存组件，进入a的嵌套子组件（如果有的话）。
     - 执行beforeRouteEnter回调函数next。

   - location.href & history.pushState & router.push区别 ？

   - params & query区别？ query在url中；刷新不会丢失query的数据

10. 自定义指令

    - 什么是自定义指令？

    - 有什么钩子函数？

      vue2还是bind、inserted、update这类钩子；vue3用created、mounted、updated代替

    - 有哪些参数？

      ```js
      (el,binding,vnode,oldVnode) => {
        el; // 操作的dom，除ta外其它都是只读！
        binding.value; // 传递的值，v-focus="1 + 1"
        binding.oldValue; // 之前的值
        binding.arg; // 传递的参数，v-focus:foo
        binding.modifiers; // 修饰符对象
        vnode; // 虚拟节点
        oldVnode; // 上一个虚拟节点
      }
      ```

    - 如何定义？

      ```js
      // 全局定义：
      Vue.directive("focus",{
           mounted: (el) => el.focus()
      })
      
      // 局部定义
      export default {
        directives: {
          focus:{
             mounted: (el) => el.focus()
          } // 在模板中用v-focus启用
        }
      }
      ```

    - 使用场景？

      防止表单重复提交，按钮权限控制，实现图片懒加载

    - 不适用任何钩子函数的情况下，默认会执行哪些钩子函数？

    - 在组件上可以使用吗？

      - 不使用钩子函数下，会在mounted和updated时调用
      -  可以但不建议，会被应用在组件的根节点

### 2024年9月23日

#### 1. Vue

1. vue & react区别？	

2. vue2 & vue3区别？

   - proxy
   - 组合式API
   - TS编写
   - v-if & v-for优先级，自定义指令生命周期
   - 生命周期

3. mixins

   - 是什么？
   - 本质
   - 怎么用
   - 场景
   - 组件和mixin重名，哪个优先级更高？ 组件
   - vue3的setup写法下有吗？没有，vue3是组合式API不推荐使用mixin，用的话得在setup外面用：mixin:[myMixin]

   答案：

   - 混入，用来抽离组件间共同功能的js对象
   - js对象（可以像vue文件中的script一样，写data，methods，生命周期函数）
   - 写一个minxins的js文件，在vue文件中导入并使用：mixins:[myMixins]
   - 日期格式化、金额格式化；请求拦截、响应拦截函数

4. SSR

   - 是什么？
     -  <img src="C:\Users\21174\AppData\Roaming\Typora\typora-user-images\image-20240924015722887.png" alt="image-20240924015722887" style="zoom:67%;" />
     - 在执行 JavaScript 脚本的时候，HTML页面已经开始解析并且构建DOM树了，JavaScript 脚本只是动态的改变 DOM 树的结构，使得页面成为希望成为的样子，这种渲染方式叫动态渲染，也可以叫客户端渲染
     - 服务端渲染就是在浏览器请求页面URL的时候，服务端将我们需要的HTML文本组装好，并返回给浏览器，这个HTML文本被浏览器解析之后，不需要经过 JavaScript 脚本的执行，即可直接构建出希望的 DOM 树并展示到页面中
     - 一句话：把 js动态修改dom交给服务器来做，浏览器只负责解析html并渲染
     
   - 优点？
     - SEO，原来爬取到的就是一个空的index，展示的页面是后续动态加进去的；有了SSR就能爬取到一个完整的HTML
     - 首屏优化
     
   - 缺点？
     - 服务器压力大
     - 开发限制：只支持beforeCreate & created2个生命周期
     - 一些扩展库需要特殊处理，需要在服务端用node.js来运行
     
   - 怎么做？

     https://chlorinec.top/2023/09/21/Development/ssr-from-scratch/#%E5%AE%9E%E7%8E%B0%E7%AE%80%E5%8D%95%E7%9A%84SSR%E6%A1%86%E6%9E%B6

   - 细节：
     1. 使用`ssr`不存在单例模式，每次用户请求都会创建一个新的`vue`实例

5. vue性能优化

   - 请求
   - 渲染
   - 打包

6. 生命周期

   - 有哪些？

     （注意vue3的destroyed换成了unmounted）

   - 各自发生了啥？用在哪些场景？

     beforemount，mounted

   - keep-alive加了哪两个生命周期？

   - 父子组件的生命周期顺序？

   - 兄弟组件的生命周期顺序？

      <img src="C:\Users\21174\AppData\Roaming\Typora\typora-user-images\image-20240924023732923.png" alt="image-20240924023732923" style="zoom:70%;" />

   - beforeCreated做了什么？ 创建组件实例

   - setup层属于哪一个生命周期：Vue 3 去掉了 beforeCreate 和 created，引入了 setup 函数，setup就是组件实例化执行的

7. 组件通信

   - $emit、$on在vue3中还能用吗？ 不能，vue3移除了$on这个Api，用mitt插件实现EventBus$emit、$on在vue3中还能用吗？ 不能，vue3移除了$on这个Api，用mitt插件实现EventBus
   - $emit、$on原理？一个公共bus，emit存放回调函数，on遍历查找回调函数并执行，拿到其中存放的值
   - 对vuex的理解？是什么？一个仓库
   - vuex和pinia的区别？
     - pinia取消了mutations，可以直接通过actions或state修改值
     - pinia的store是独立的，可以分多个小的库来存储数据；vuex的store是一个整体，需要注册不同模块来区分
     - pinia更契合vue3的组合式API，每个store用于一个功能模块；vuex仍然是基于optionAPI，作为一个整体库来存储数据

### 2024年9月25日

#### 1. Vue

1. vuex？
   - 组成？
     - mutation 怎么触发，修改state的唯一方式
     - action怎么触发
   - action & mutation区别？
   - 为什么要先触发actions，actions再触发mutation？
     - actions 负责处理业务逻辑，而 mutations 只专注于状态的直接变更，增强了代码的可维护性。
   - vuex & localStorage区别？
   - vuex & redux区别？
   - 为什么要用vuex？
   - 为什么mutation不能做异步？[（Vue）解密 Vuex：为什么 Mutation 不宜异步操作？ - 知乎 (zhihu.com)](https://zhuanlan.zhihu.com/p/673206125#:~:text=在Vue.js的状态)
   - 如何批量使用getters？

2. vue3？
   - proxy如何做到能监听整个对象，能支持数组api？
   - 什么是组合式API？

3. 虚拟dom？
   - 虚拟dom解析过程？
   - 虚拟dom缺点？ 首次渲染大量DOM时，由于多了一层虚拟DOM的计算，会比innerHTML插入慢。
   - diff算法？
   - key的作用？
     - 对于v-if
     - 对于v-for
   - 为什么不用index用key？

4. 如何做跨域？

   - 为什么要有跨域？同源策略限制了什么？

     1. 访问不同源的cookie
     2. 访问不同源的本地存储
     3. 它限制了在不同源之间的 XMLHttpRequest 和 Fetch 请求。

   - 方案？

     1. JSONP

     2. cors

     3. http proxy（本地用代理服务器发请求）

        ```js
        let path = require('path');
        let HtmlWebpackPlugin = require('html-webpack-plugin');
        module.exports = {
            mode: 'production',
            entry: './src/index.js',
            output: {
                filename: 'bundle.min.js',
                path: path.resolve(__dirname, 'build')
            },
            devServer: {
                port: 3000,
                progress: true,
                contentBase: './build',
                proxy: { // => 以'/'开始的请求，就把请求路径转到 target
                    '/': {
                        target: 'http://127.0.0.1:3001',
                        changeOrigin: true // => 允许跨域
                    }
                }
            },
            plugins: [
                new HtmlWebpackPlugin({
                    template: './src/index.html',
                    filename: 'index.html'
                })
            ]
        };
        ```

     4. 反向代理（打包时用代理服务器发请求）

        [使用nginx反向代理解决请求接口跨域问题（前端）_axios 拦截和ngnix反向代理-CSDN博客](https://blog.csdn.net/small_cutey/article/details/121932691?spm=1001.2014.3001.5502)

        [nginx 本地部署前端打包编译后项目_更新前端打包文件需要重启ngnix吗-CSDN博客](https://blog.csdn.net/small_cutey/article/details/121772890?spm=1001.2014.3001.5502)

     5. websocket































- 代码能力：大厂基本都会考一道算法题（简单、中等）
- 薪资：现在很多公司都在卡涨幅，除非是特别优秀的同学
- 经典问题：项目中遇到比较有难点、挑战的问题，怎么去攻克、解决的
- Promise 必问，最好能手写出来，并且理解每一行代码的含义






1. 学好分片上传：分片+hash+webworker（arraybuffer结构化克隆+复用worker）

   - 中断上传怎么办？
   - 怎么秒传？并发

2. 虚拟滚动怎么做？加上节流函数？

3. AbortController中断请求，可以怎么优化？中断请求是否会释放资源？如何处理中断后需要重新发起请求的情况？AbortController` 和 `CancelToken` 的区别？

4. 封装的ELMessageBox怎么实现复用？怎么封装的？如果有自定义的内容怎么办？

5. websocket相关知识，写的时候详细放进去

   

   

   

   

 	 	
